#electoral class="ev" data-clinton-images=""
  .ev__header
    h2.ev__title - ELECTORAL VOTES
    .ev__blurb
      p - The election of President and Vice President of the United States |~
          is an indirect election in which...
  .ev__content
    canvas#static-background.canvas-onion
    canvas#ev-canvas.canvas-onion
    canvas#static-overlay.canvas-onion.canvas-overlay
<script>
  var electoralImages = { 
    clinton: {
      basic: "${data.path_to_asset('digest', 'images/splash/clinton.png')}",
      sad: "${data.path_to_asset('digest', 'images/splash/clinton-sad.png')}",
      happy: "${data.path_to_asset('digest', 'images/splash/clinton-happy.png')}"
    },
    trump: {
      basic: "${data.path_to_asset('digest', 'images/splash/trump.png')}",
      sad: "${data.path_to_asset('digest', 'images/splash/trump-sad.png')}",
      happy: "${data.path_to_asset('digest', 'images/splash/trump-happy.png')}"
    }
  }
</script>
