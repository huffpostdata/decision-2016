<!DOCTYPE html>

html lang="en"
  head
    meta charset="utf-8"
    title - Splash Page Test
    meta name="viewport" content="width=device-width, initial-scale=1"
  body
    p - This is a test splash page! here are the divs
    #election-splash-refresh
    #electoral_votes
      <!-- this markup will be generated by JS -->
      .bubble.bubble--clinton 
        span.bubble__candidate - CLINTON
        span.bubble__votes - 0
      span.bubble-handle.bubble-handle--clinton
        span.bubble-handle__inside
        span.bubble-handle__border
      .bubble.bubble--trump
        span.bubble__candidate - TRUMP
        span.bubble__votes - 0
      span.bubble-handle.bubble-handle--trump
        span.bubble-handle__inside
        span.bubble-handle__border
      .face.face--clinton
      .face.face--trump
      .bars
        .bar.bar--clinton
        .bar.bar--trump
    #election_map
    #battlegrounds
    #election_seats
      #house_seats
      #senate_seats

    <!-- <script> src is the "page" (views/splash.js.marko), not the JS -->
    script src="${data.path_to('splash.js')}"
